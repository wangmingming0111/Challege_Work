import React from 'react';
import API from '../api';
import '../User.css';

export default class PersonTitle extends React.Component{
  render() {
    return(
      <div className='Title'>
      </div>
    )
  }
}